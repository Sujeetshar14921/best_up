{
  "name": "bestup",
  "version": "1.0.0",
  "description": "BestUp - Find your perfect smartphone",
  "scripts": {
    "build:all": "npm run build --prefix frontend && npm run build --prefix admin_dashboard",
    "install:all": "npm install --prefix frontend && npm install --prefix backend && npm install --prefix admin_dashboard",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:admin": "npm run dev --prefix admin_dashboard"
  }
}
