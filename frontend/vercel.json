{
  "version": 2,
  "buildCommand": "npm install && npm install --prefix ../admin_dashboard && npm run build && npm run build --prefix ../admin_dashboard && node -e \"const fs=require('fs');fs.rmSync('dist/admin',{recursive:true,force:true});fs.cpSync('../admin_dashboard/dist','dist/admin',{recursive:true});\"",
  "outputDirectory": "dist",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/admin",
      "dest": "/admin/index.html"
    },
    {
      "src": "/admin/(.*)",
      "dest": "/admin/index.html"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
